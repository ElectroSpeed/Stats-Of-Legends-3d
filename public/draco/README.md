# Décodeur Draco

Placez ici les binaires WASM du décodeur Draco (`draco_decoder.wasm`,
`draco_wasm_wrapper.js`, etc.), servis localement plutôt que via un CDN tiers.
Source : `node_modules/three/examples/jsm/libs/draco/`.
